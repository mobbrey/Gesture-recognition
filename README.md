# Gesture-recognition
Sistema de control de motores DC mediante gestos de la mano en tiempo real. Usa MediaPipe para detectar gestos con la cámara del PC, los procesa con Python y envía comandos por WiFi HTTP a un ESP32 en modo Access Point, que activa los motores a través de un driver L298N Mini. 
